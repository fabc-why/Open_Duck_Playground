simple instruction

このフォルダには、openduckをros2で操作する部分のプログラムです。
たとえばxbox.pyはxboxコントローラで操作できます。

ただ、このコード単体で実行しても意味はありません。

mujoco_infer_ros2.pyも別プロセスで動かしておく必要があります。
hostとportをいい感じに設定（デフォはlocalhost）すれば、いい感じに動くでしょう。

つまり、mujoco_infer_ros2.pyを動かして、別プロセスでxbox.pyを動かして、ros2の仲介サーバーも動かせば、ちゃんと使えるでしょう。
